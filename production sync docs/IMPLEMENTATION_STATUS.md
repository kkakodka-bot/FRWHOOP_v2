# Production sync implementation status

Status: IN PROGRESS. Integrated baseline, scoring durability and compression checkpoints are committed. Remaining client integration, full historical computation and independent reviews are active. Device and deployment acceptance remain unproven.

### Independent repair reruns, 2026-09-18 10:49 local

- S04 records ten exact historical-state/metric-admission/main-night/wall-hour files. Root read all seven production and three regression files and rechecked the partition hashes; their three suites in the preserved current JVM archive pass **19 tests, zero failures/errors/skips**. Warm/cold reset and calendar-window review evidence remains bounded to those contracts. This checkpoint intentionally precedes S03 while Android's mixed provenance/DAO peer is held for GPS repair, so S03/S05-S07 remain explicit forward dependencies; no independently buildable intermediate tree or current-source full-day parity is claimed.
- Root's original, unchanged native preference-permit regression now passes **1 test, zero failures,0.015s** against the frozen repair. A separate fresh foundation run passes **26 tests, zero failures/skips,3.533s**, including ten actual SIGKILL writer/reopen exits and five successful third-process verifiers. All six source hashes and the original negative probe/log are unchanged relative to their respective manifests. The transaction-scoped observer rechecks the captured permit and cancellation at native commit entry and is removed afterward; no atomic authorization-lock-through-fsync claim is made. Independent repair-delta review remains active; actual completed-action app bindings and signed-out durable ownership are not covered. Evidence: `root-preference-permit-review/repaired.log`, `root-preference-intent-foundation-repaired.log`, `SWIFT-PREFERENCE-INTENT-PERMIT-REPAIR-FREEZE.md`.
- Root independently reran the image-provenance P2 repair: **113 offline tests, zero failures/skips,38.071s**, plus Mill's unchanged five-control reproduction **5 pass, zero failures/skips,1.289s**. The previous common-config-only check omitted effective worktree configuration; the repair checks both supported scopes before source-object access while retaining offline guards. Original negative evidence is preserved. These are local Git and mocked external-operation controls, not a real image build/registry/deployment qualification. Evidence: `root-scorer-image-p2-offline.log`, `root-scorer-image-p2-independent-controls.log`.
- Root freshly compiled the synchronous record-admission sources and original20/new26 tests: **46 tests pass,0.553s** in the standalone host harness. The initial execution failed because the retained dependency JAR omitted one actual AccountScope nested class; the complete five-class cached dependency fixes harness packaging without source/assertion changes, and both logs remain. This is not a fresh Android/Room gate. Record admission is volatile until publication; the separately allocated cold-recovery slice must retain unknown/torn bytes while recovering verified healthy siblings. Evidence: `root-record-admission.KL5Go1/initial.log` and `complete-dependency.log`.
- Root registered the separate GPS delivery artifact in undeployed Room43 and all shared schema/registry copies. A raw-callback destination factory configures WAL/FULL before create/migrate transactions; active-writer durability checks are required at GPS commit and settlement. Eight new native durability methods target SDK26/34 but have not yet run. GPS payload/witness/revision repair and the combined fresh KSP/native/full-suite gates remain pending. Current configuration is not proof that older preexisting rows survived physical power loss or that every pooled connection stays pinned forever.

### Preference-intent and image-provenance review, 2026-09-18

- Swift's six-file preference-intent foundation passes root's fresh22-case native run, including ten actual SIGKILLs and five verifier processes. A separate independent regression then confirms a P1: revoking the captured permit from the actual GRDB will-commit generation predicate, after the two closure-body permit checks, still commits one intent, two children, one projection and head sequence1. Its original1-test/5-assertion failure and database are retained; author repair/native re-review are active. No live preference binding or all-mode durable-save closure is claimed. Evidence: `ROOT-SWIFT-PREFERENCE-INTENT-INDEPENDENT-REVIEW.md`, `root-preference-intent-foundation.log`, `root-preference-permit-review/initial.log`.
- Scorer image-provenance source is frozen at11 explicit paths. Root independently reran all97 offline tests:97 pass, zero failures/skips,38.024s; all11 hashes match (`root-scorer-image-offline-current.log`). Independent source review remains active. No real Docker daemon/build, registry, SSH, publish or deployment was used. The phase3 native `--local` command is unchanged and still awaits the shared Android freeze; no deployed image is qualified.

### Room43 and GPS current-source native gate, 2026-09-18

- A04's16 sender/receipt paths remain byte-identical to their owner freezes. Root reviewed the negotiated identity/scalar codec, prepared auxiliary-prefix, scoped progress and pure receipt-parser boundaries; all seven topic test classes in the preserved full-run archive pass **55 tests, zero failures/errors/skips**. This topic is checkpointed before A03 while A03's mixed DAO/repository files await GPS repair. It deliberately retains those forward dependencies and is not an independently buildable commit. The new receipt parser is data validation only: no Android durable receipt queue, exact capture archive transport, cleanup permission or producer-retirement closure is inferred.
- `1232603` records A01's exact11 identity/fence/build paths. The following A02 topic records only the namespace mutation lease, captured-owner backup/restore and its10-case native regression file; all three still match the independently reviewed final hashes. All10 pass in both the129-case target and the5,867-case full-run archives. The shared Room implementation remains a forward dependency in A11. This closes the reviewed stale-account restore mechanism, not a new power-loss-atomic database-plus-preferences backup format or physical restore acceptance.
- Capture9/Room43/registry independent review reports no additional P0/P1 within that bounded slice and corroborates the archived129-pass and repaired Android10/Swift10 evidence. Cold torn-file recovery, actual producer/runtime construction and archive/receipt/prune remain open. A bounded synchronous immutable-record admission prerequisite is now allocated; admission in memory is explicitly not a durable acknowledgement.
- The GPS follow-up repair is allocated to retain the complete immutable ordered workout payload in Room alongside an exact canonical-HR projection witness, preserving first-writer behavior without overwriting raw rows. Shared schema/destination durability integration and new native regressions remain pending; the earlier33 passing GPS tests do not close the new producer mismatch or revision finding.
- Android's remaining139 paths are partitioned into11 focused topic review checkpoints in `ANDROID-CHECKPOINT-REFRESH/README.md`, with exact disjoint allowlists. These deliberately retain forward references and are **not individually buildable, deployable or independently qualified commits**; only the final integrated candidate receives the combined gate. A01 contains11 account identity/auth/storage/fence/build-dependency paths, all still byte-identical to the original frozen inventory. Root inspected their current production and native test boundaries; its full-run archive includes16 account-identity,4 Room-revocation and2 commit-linearization tests passing. Native Android Keystore/physical process/locked-device acceptance is not inferred from synthetic controller and Room tests. GPS and mixed runtime companions stay held for their open repair.
- The following full Android rerun completed in4m4s and built the debug APK: **5,867 tests across711 suites, one failure, zero errors, six existing skips** (`root-room43-gps-full.log` and archived XML). Sole failure: shared cloud-ingestion registry lacked the two new local index tables. Root added explicit Android-only/local-only entries to both identical registry copies, without exempting raw capture delivery or changing a test. Follow-up **10 Android tests, zero failures/errors/skips;10 Swift schema/registry tests, zero failures/skips** pass (`root-room43-registry-repaired.log`, `root-room43-swift-registry-repaired.log`). These targeted results do not convert the failed full run into a pass. APK SHA256817baec8b320c47df1955906c9a7522bf85d7a021c42174d7c5f55f002baec6c; not installed or release-qualified.
- Independent GPS repair review confirms the original restore/retry data-loss mechanisms are addressed, but finds a new P1: ordinary smoothed or unequal same-second captured HR cannot satisfy exact equality against the canonical raw-HR table, permanently blocking valid completion. A P2 also bypasses the repository revision used by Sleep diagnostic readouts. Source-backed repros are not yet executed. A lossless captured-versus-destination representation and post-commit revision repair are being designed; no deletion predicate will simply be removed. Evidence: `GPS-FINALIZATION-P1-REPAIR-INDEPENDENT-REVIEW.md`.
- Additive Room43 registers two Android-local capture-index tables without replacing existing data. Both shared schema-oracle copies explicitly describe their exact Android-only shape and remain byte-identical; all previous fixture values are structurally unchanged except roomVersion. Swift schema guard: **7 tests, zero failures/skips,0.043s**. No GRDB migration was added or renamed.
- Root's fresh KSP/Android build and combined native run passes **129 tests across13 suites, zero failures/errors/skips,52s build** (`root-room43-gps-finalization-native.log`, archived XML alongside). Includes20 journal,15 index,5 commit fence,3 true42->43/restore,4 schema,4 migration-chain,33 GPS,10 restore,9 profile,17 runtime and9 W1 identity/provenance tests. All frozen source hashes match. Standalone cached-dependency results are not substituted for this fresh run.
- GPS finalization now freezes all workout/HR payload fields before destination writes and checks the current exact workout/HR under the namespace and SQLite transaction through recovery-debt deletion. Twenty new tests exercise crash/reopen retry, actual synthetic older restore, conflicting destination, revocation and writer/restore ordering. Independent source closure remains pending, including repository readout revision integration; no physical crash/location/background gate is claimed.
- Capture files remain the recovery authority. Actual synthetic older42 restore preserves files and reindexes exact identities only under a fresh admitted writer. This does not yet integrate generic-HR/IMU producers, cold-worker inventory, typed archive receipts or pruning. Full Android suite/APK and independent source reviews are active; the earlier failed5,804-test verdict is not replaced by this targeted pass.
- `c5c0a64` records the separate seven-file Swift context-adapter/native-harness checkpoint. No push, deployment, production data mutation or user database restore occurred.

### Fresh app and prepared-upload checkpoint, 2026-09-18 09:49 local

- `12647fc` records the prepared-upload checkpoint. The following four-file automatic-sleep timezone checkpoint has independent fresh-build coverage: **16 tests, zero failures/skips,43.742s**, alongside the author's complete **2,000 tests, zero failures/skips,260.414s** at the same four hashes. Root checked the exact production diff and final hashes; its fresh app build also compiles those APIs. Explicit named zones now resolve onset and midpoint separately and participate in the memo key; nil/default behavior and both historical corpus bytes remain unchanged. This is an additive API gate, not current-source full server_day composition or changed shipped activation. Evidence: `W4-AUTOMATIC-SLEEP-DST-INDEPENDENT-REVIEW.md` and `W4-AUTOMATIC-SLEEP-DST-CHECKPOINT.md`.
- W5 prepared selection and its live-reservation repair are independently closed for the exact frozen source. The original unchanged two-test reviewer reproduction now passes in a freshly built private harness; original failures remain preserved. Root verified all14 final source/test hashes, including the two superseding journal/test hashes and separate UTC day-selector correction. Unknown or duplicate debt remains retained and fail-closed; there is no downgrade, automatic repair or physical disk-capacity guarantee.
- Root rebuilt the actual current macOS app/test bundle successfully after the recorded TodayView binding typo and test-only missing NoopPush import were corrected. The first disposable test-host launch was interrupted before XCTest because companion PackageFrameworks/resource bundles had not been copied. Adding those exact fresh build products repaired packaging without changing sources, assertions or OS permissions. Failed and successful logs remain separate.
- Fresh app-host execution passes **217 tests, zero failures/skips,34.418s** (`root-current-prepared-distance-bundled-app-tests.log/.xcresult`). It includes25 prepared-selection tests plus the two original review regressions, receipt/queue/IMU/account/consent groups, all22 AppModel context tests, nine distance and seven interoperability tests. Separate fresh readback execution passes **122 tests, zero failures/skips,0.570s** (`root-fresh-readback-app-tests.log/.xcresult`). Root separately reran the current NoopPush package: **78 tests, zero failures/skips,4.036s** (`root-noop-push-current.log`). These are host correctness tests, not iPhone background-daemon, locked-file, device performance or deployed receiver acceptance.
- The illness-distance AppModel two-file binding is independently source-reviewed with no confirmed P0/P1/P2; the22 tests above close its fresh app-host gate. Actual populated fixture hash remains429dc04ef7287467665eaf8758570a4d317ac3a2f1fcd814357c6469643e00f7. This does not activate scoring, notifications or a device release.
- `7fa5fe1` records the four-file automatic-sleep API checkpoint. A separate seven-file Swift context checkpoint preserves the independently reviewed optional distance DTO fields, typed adapter, seven actual-JVM-fixture interoperability tests, nine synthetic malformed/ownership/legacy controls and the explicit native harness. Root's7 native and the fresh app's7+9 executions pass. Its three JVM producer/test peers stay in S06/S07; the broader AppModel binding stays with client integration. Optional missing fields remain unavailable, and distance never becomes an alert gate.
- This focused checkpoint contains only the14 prepared-upload/UTC paths plus this ledger. Other tested app and scoring changes remain in their separately owned checkpoints; the fresh full-tree run does not prove every intermediate commit independently builds. GPS P1 repairs, capture migration/runtime integration, current-source full-day parity, ordinary preference intent durability and external acceptance remain open.

### Current native integration checkpoint, 2026-09-18

- Root reran the exact current service/kernel sources with PostgreSQL18 and the actual Swift auxiliary fixture: **256 service tests, zero failures/errors/skips; 1,015 kernel tests, zero failures/errors, five existing skips; installDist successful**. The combined command completed in70s. Logs and archived XML: `root-scoring-full-current.log` / `root-scoring-full-current-junit.tgz`; disposable artifacts: `root-scoring-full-current.oLh6bL`. All85 paths in the reviewed scoring partition still match their pinned hashes after execution. This excludes the separately mandatory current-source whole-day Swift parity task; no full server_day or production acceptance is inferred.
- Scoring checkpoints are partitioned by dependency and responsibility in `SCORING-CHECKPOINT-PARTITION.md/.json/.sha256`. The first cut contains13 pure-kernel API/extraction and rounding-test files only. The full-tree result above is not an assertion that every intermediate commit was independently built. Later cuts require the Android StepSample provenance peer and the explicitly included synthetic Swift context fixture; the ignored personal-fixture tree must not be staged wholesale.
- `2c594b4` records that pure-kernel cut. The next six-file history-queue cut adds explicit history registration/claims, predecessor and generation metadata, checkpoint-only history publication, repeatable-read predecessor loading, and authenticated RPC/invalidation/sleep-identity regressions. It uses the already-committed additive010/020/030/050 migrations; it does not modify migration history or activate the history scorer. Root inspected the exact production diff and all three added test files; the current256-test service result includes them.
- `b294153` records that queue/checkpoint cut. The separate three-file Swift test-only history checkpoint contains schema2 input resolution and actual baseline/sleep-debt helper orchestration, including warm/cold reset repair. Root's prior100-test history run and the later complete2,000-test analytics run include these exact sources; the independent warm-reset probe agrees at HRV age10 and unaffected RHR age19. Internally constructed observations remain trusted test/composer inputs, not authenticated archive imports, raw-to-result evidence, a full server_day composer or a current-source corpus export.
- The full Android test/APK command built the debug APK but failed: **5,804 tests, two failures, six skips** (`root-android-gps-restore-profile-full.log`, archived XML alongside). Both failures attempted to reuse a generation after the process-teardown close had retired it. The two fixture repairs now explicitly assert old-generation rejection and same-owner fresh-generation reopening, retaining all migration/provenance/wire assertions. Root's follow-up passes **41 tests, zero failures/errors/skips**, including10 restore,9 profile,9 W1 migration/provenance and13 GPS cases (`root-android-generation-reopen-repaired.log`, archived XML). This focused pass does not replace the failed full-suite verdict.
- The earlier67-test repaired archive contains nine restore cases; the additional unwrapped production-lock control is included in the latest10-case run, not retroactively in that archive. No production fence was weakened to permit stale-generation reopening.
- Independent GPS review found two remaining P1s despite the13 passing journal tests: same-generation restore between destination save and GPS cleanup, and crash/reopen retry rebuilding a committed workout with lost HR/score fields. Immutable final-payload persistence and current-destination settlement proof are active repairs. GPS durability is not independently closed. Evidence: `ROOT-ANDROID-GPS-INDEPENDENT-REVIEW.md`.
- The W5 live-reservation repair passes99 actual-source helper and78 package tests, retaining the independent failing reproducer unchanged. Exact two-file re-review and a fresh app-host run remain required. Unknown or duplicate selection debt is retained and blocks admission; fault-injected host tests are not physical disk/background-daemon evidence.
- The seven-file illness-distance extension is independently reviewed without a confirmed P0/P1/P2. Root's fresh7-test native interoperability compile passes against the actual366,267-byte JVM fixture (SHA256429dc04ef7287467665eaf8758570a4d317ac3a2f1fcd814357c6469643e00f7). Separate independent execution passes16 tests. The subsequent two-file AppModel binding is frozen with22 app tests awaiting the fresh app build; no notification, activation or device claim follows.

### Active integration checkpoint, 2026-09-18 09:04 local

- Local checkpoint `6631f32` preserves the five-file historical-provenance repair. The following separate eleven-file checkpoint adds only opt-in calendar/date/zone APIs and their six regression files; omitted/nil legacy defaults remain unchanged. All eleven independent-review hashes were rechecked before staging. No server_day composer, current-source corpus, activation or automatic-stager change is included in that checkpoint.
- The five additive Swift calendar/timezone APIs and six new test files are frozen and independently reviewed without a confirmed P0/P1/P2. Their focused native run passes217 tests, including32 new cases and byte-identical live replay of13 historical v1 plus13 v2 cases. Root's repaired full analytics run passes **1,984 tests, zero failures/skips,210.584s** (`root-full-analytics-historical-gates-repaired.log`). The initial nine failed assertions in one historical-source comparison test remain preserved. All five test-only provenance files are independently closed: eight native helper/writer tests plus five API-probe tests pass; all218 advertised current probe hashes match and all non-hash fields equal the original probe. No frozen corpus or current-source JVM source-hash gate is relaxed. Evidence: `W4-FIVE-SWIFT-CALENDAR-SEAMS-CHECKPOINT.md`, independent review, `W4-HISTORICAL-PROVENANCE-FIFTH-FILE-CLOSURE.md` and `ROOT-HISTORICAL-NO-FETCH-REPAIR.md`.
- The actual Git missing-promisor regression reproduces unintended fetch subprocess starts with all transports denied. Explicit no-lazy-fetch repair passes all six helper tests, including432 exact historical source blobs, in36.511s. This is offline test provenance, not new whole-day production acceptance.
- Independent W5 prepared-selection review finds a P1: a caught post-write reservation error retains a selection on disk but loses its live lane barrier. Repair and same-process retry coverage are active; do not treat the earlier76/92 native results as closure. Separately, the UTC day-selector codec defect is fixed:78 package and93 helper tests pass, including the unchanged failing probe and four isolated timezone processes.
- The populated context producer now emits both actual illness-distance extension fields. Fresh JVM10 tests and native Swift16 tests pass. The new366,267-byte fixture SHA-256 is429dc04ef7287467665eaf8758570a4d317ac3a2f1fcd814357c6469643e00f7; old artifacts remain immutable. AppModel distance binding and fresh app-host acceptance remain open.
- Fresh app compilation exposed a root HRV preference binding typo in TodayView (`model` instead of its existing `app` environment object). The one-line correction is applied; no successful rebuild is claimed yet. The failed log is retained.
- Android P1 repairs are active. Root's owner-bound GPS SQLite journal and async session/UI/service wiring retain route debt until workout commit or explicit discard, reopen paused, and preserve exact ordered points. Thirteen new real SQLite/Room tests are written but unexecuted. Banach owns shared capture journalling; Rawls's six-file restore/profile barrier checkpoint is frozen with18 unexecuted native tests. No existing failing gate is removed and no physical lifecycle evidence is claimed.

## Authority and bootstrap

- User confirmed on 2026-09-18 that the numbered files are authoritative.
- Original policy: `/Volumes/Untitled/WHOOP NARA-pr16/production sync docs/AGENTS(3).md`, 68 lines, SHA-256 `759d0db4b8156f96ac6fe3bc84058154150f8722b959f73bf8e757cbe0f3660d`.
- Original specification: `/Volumes/Untitled/WHOOP NARA-pr16/production sync docs/spec(3).md`, 266 lines, SHA-256 `cb65bf457a8068c58f8cfcd4f6c09eff16215c74e8e5b97e3544994f8009ba05`.
- Both originals were untracked (no tracked Git blob ID). Copies here retain the exact bytes.
- Committed copy blob IDs: policy `c3c608068ec392d38f3ad8975944c813d3e4d8ab`; specification `7f02464a81a617b267fae7843ba89129d15a1da5`.
- The older owner-only/local-compute scope is superseded by the user-authorized multiuser/server specification. No production mutations, push, PR publication, deployment, phone reinstall or database reset are authorized.

## Isolation and ancestry

- Worktree: `/Volumes/Untitled/WHOOP NARA-production-sync-2026-09-18`.
- Branch: `codex/production-sync-2026-09-18`.
- Base: live `origin/main` = `34fc950f03199b9d26e5019311394cb49cc34c7c`.
- Live PR15: `331f339bda78cc739c849cf3a43e3ba4a8722696`, base `fix/offload-pipeline-correctness`, open.
- Live PR16: `5caa31689da0023e111beb36850d3f81d67e1be2`, base `main`, open.
- Their merge base is the main SHA above; neither line contains the other. No foundation branch containing both exists in the inspected refs.
- Origin's legacy URL redirects from `kkakodka-bot/FRWHOOP_v2` to the requested `kkakodka-bot/naraWhoop`; GitHub metadata was queried directly on the requested repository.
- Integrated all non-merge commits from both lines with source commit trailers. PR16's resulting tree at `650b993` was identical to its live head. The original checkout and its untracked documents/builds were preserved.
- Conflict resolution retains `v46-rr-source-index`, `v47-server-score-cache`, and `v46-ppg-record-identity` with original migration bodies. Shared schema fixtures include both v46 identifiers.
- Baseline cherry-picks in order: `37b9e42`, `50a7e0d`, `fc6006c`, `650b993`, `35489ec`, `188c2f4`, `7af5e1c`, `138dd11`, `5e930aa`.

## Execution and evidence

Six read-only scouts cover ancestry/migrations, BLE/storage, identity/privacy, server/intake, iOS/readback/performance, and tests/operations. Implementation ownership is assigned only after their findings; the integration lead owns shared contracts and migrations. Independent review and targeted tests precede each phase completion.

Local artifacts: `/Volumes/Untitled/nara-production-sync-evidence-20260918`. Internal disk had only 7.3 GiB free at preflight; builds use the external SSD (500 GiB free). Xcode 26.3 build 17C529 is installed. Deno was absent from PATH at preflight.

| Command | Result / evidence |
|---|---|
| `git fetch origin`; `git fetch origin refs/pull/15/head refs/pull/16/head`; `gh pr view 15/16 --repo kkakodka-bot/naraWhoop --json number,title,state,headRefName,headRefOid,baseRefName,mergeCommit,url` | Passed; heads recorded above |
| `swift test --package-path Packages/WhoopStore --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/store-build` | Initial 555 tests: four failures from inherited cache registries and integrated migration fixture; repaired rerun 555 tests, zero failures, one existing device-copy skip, 3.842 s. Logs phase0-store.log and phase0-store-repaired.log. |
| `swift test --package-path Packages/WhoopProtocol --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/protocol-build` | 735 tests, zero failures, two existing skips, 0.536 s; phase0-protocol.log. First launch failed before execution due to artifact-directory creation race; rerun after directory existed. |
| `swift test --package-path Packages/WhoopStore --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/store-build --filter IntegratedMigrationTests` | Two predecessor-schema/repeated-migration tests passed in 0.025 s; phase0-upgrades.log. These in-memory tests do not establish disk crash/reopen durability. Existing research-schema test separately uses a file-backed database. |
| `git diff --check` | Passed for integrated tracked baseline |
| `Tools/quality-gate.sh` discovery | Absent; no gate claimed. Existing infra acceptance scripts are under inspection. |

### Phase 0 review and repair

Independent reviewer Pauli (01a0b36f-3c9f-7293-a602-360f989e7007) found no blocking baseline integration defect. Confirmed the 48 migration identifiers and unchanged migration bodies, fixture byte parity and cache classification. Fixed the review finding that an incomplete migration list could trap the oracle test; added explicit PR15 waveform-byte preservation. No existing test was deleted or skipped. The old numeric-prefix assumption was replaced with exact preserved deployed identifiers and sequential checks for future migrations; renaming deployed identifiers would violate W0.

### Dependency order and ownership

1. Baseline integration and migration fixtures (root; phase 0).
2. In parallel: BLE durability/retention (Maxwell), account credentials/upload admission (Banach), JVM queue/result/archive durability (Mill), Edge intake/ownership/object repair (Rawls). Root owns shared GRDB registrations, storage lifecycle and integration.
3. Readback/metric ownership/cache/Today/Sleep (Bacon) consumes the agreed account and result contracts. Background file transfer (Pauli) has exclusive transport ownership after phase-0 review.
4. Root integrates lifecycle, source ownership, performance instrumentation and operational gates. Re-run relevant suites and independent reviews after each phase, repair findings, then checkpoint.

No agent may modify another owner's files. Server and Edge changes use separate additive migration files, integrated by root. Schema migrations already shipped remain unchanged.

### Phase 1 integration in progress

- Root registered additive GRDB migrations v48 scoped result cache, v49 durable ingest/receipt associations and v50 immutable local account ownership. Both schema-oracle copies and all three cloud-registry copies include these tables; deployed migration bodies remain unchanged.
- AccountStorageLayout uses separate project/user directories and preference suites. Existing unowned stores are preserved and are not silently assigned to a signed-in account. Repository writers validate a persisted owner before use; a different account or project cannot rebind one.
- AccountAppRuntime replaces the model/storage/BLE/HealthKit presentation runtime on identity generation changes. Widget snapshots carry an owner stamp and are hidden on logout. Lifecycle integration and app compilation remain in progress, not accepted.
- Root command: `swift test --package-path Packages/WhoopStore --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/store-build --filter 'AccountOwnershipTests|IntegratedMigrationTests|SchemaOracleTests|CloudIngestionRegistryTests'`: 15 tests passed, 0 failures, 0.142 s test run. Artifact: phase1-owner-schema-tests.log. An earlier new-test compile failed because the async store initializer was called without await; fixed without changing assertions.
- Readback worker reports 10 cache tests passed in its isolated build. Root integration rerun and independent review are still required. No UI cutover or device result is claimed from those tests.
- `xcodegen generate` passed. First unsigned Release iOS build exposed three integration compile errors (raw IMU table inference, missing readback context, capability-set reference); repaired rerun passed (`phase1-ios-build-rerun.log`). Later source changes still require a final exact-candidate rebuild. No physical installation or performance evidence is inferred.
- Receipt-backed pruning requires actual validated server receipts, not just the new local schema. Quarantine transport, server contracts, lifecycle recovery and full metric ownership are still being integrated.
- Root full WhoopStore run initially reported 574 tests, 22 failures and one existing device-copy skip (`phase1-store-full.log`). Failures include retention fixtures expecting unacknowledged deletion, pending-job counts and registry adoption coverage. The ingest owner is repairing fixtures with real receipt preconditions, preserving both prune-positive and unsent-retention assertions; final rerun is required.
- Root NoopPush full suite initially failed six assertions/errors because the inherited Apple Compression raw algorithm 9 is not Zstandard. Replaced it with the existing native libzstd shim on macOS and a bounded RFC 8878 raw-block frame encoder on iOS. Fixed the gzip output-pointer lifetime and empty input. Native libzstd independently decodes both paths in new tests (empty, boundary, multi-block and 4 MiB inputs); portable encoding does not claim a compression-ratio benefit. Full rerun: **46 tests, zero failures, 2.908 s**, `root-push-codec-repaired.log`. Independent review pending.
- Root reran `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home W3_TEST_ARTIFACTS=/Volumes/Untitled/nara-production-sync-evidence-20260918 ./gradlew :analytics-kernel:test :service:test :service:installDist --no-daemon --continue --rerun-tasks` from `scoring-service`: service **51 passed, zero skipped**; kernel **741 passed, five pre-existing skips**, zero failures. Build successful, 15 s, `root-w3-native-suite.log`; XML totals independently parsed. Disposable native PostgreSQL tests are included, not deployed evidence. Independent review identified four P1 findings; repair remains required before checkpoint.
- Root reran the exact sanitized, loopback-only Deno command documented in `supabase/functions/tests/README.md`: **59 passed (12 real PostgreSQL/PostgREST/HTTP integration steps), zero failed, 1 s**, `root-edge-native-suite.log`. No production credentials, external object bucket, device or deployment used. Independent intake review pending.
- Independent account-runtime review identified six source-traced P1 findings; root is repairing them before acceptance. W4 server history/computation and full Android runtime parity are still implementation work, not external blockers. Android API 35 SDK was located under `/Volumes/Untitled/physiology-v2-baseline.ROUXBD/android-sdk` after the initial scout.
- Local operational gate validator: `node --test infra/vps/scripts/verify-sync-evidence.test.mjs` passed seven synthetic tests. `bash -n` passed for all three acceptance scripts. `bash infra/vps/scripts/phase3-acceptance-checks.sh --preflight` exits 1/NOT_READY without the required evidence manifest, before secret or remote access; that expected rejection is not deployment acceptance.

### Reviewed checkpoints and current integration evidence

- `a1f12dc`: scoring generation/lease-fenced publication, deterministic source selection, independent archive retry and additive migrations 010000/030000. Mill's final native suite: 63 service tests and 741 kernel passes (five existing skips), zero failures. Rawls independently rebuilt and ran all 12 repair regressions against fresh native PostgreSQL: passed, zero skips. `W3-repair-independent-closure.md` closes legacy overwrite, version-registration race and future-RR leakage at the recorded source hashes. Historical W4 computation is not included in this checkpoint.
- `bedf3cf`: native macOS Zstandard, bounded RFC 8878 raw-block iOS frames and gzip pointer-lifetime/empty-input repair. The 46-test NoopPush run above and independent native decoder review cover interoperability, not compression ratio or device energy.
- Root full protocol rerun: 735 tests, zero failures, two pre-existing skips, 0.466 s (`root-protocol-durability-final.log`). Root WhoopStore initially had one old foreign-database expectation failure; repaired full rerun: **583 tests, zero failures, one existing device-copy skip, 4.269 s** (`root-store-durability-repaired.log`). The new policy intentionally preserves unknown populated stores in place. Fixtures verify repeated refusal, original rows and exact DB/WAL preservation, no rename and separate-path fresh creation. The shared oracle now pins Room 40 and explicit unknown-PPG default divergence; nine focused Swift schema/upgrade tests pass after that fixture update (`root-room40-shared-schema.log`). Android integration is a following checkpoint.
- Real writer durability review found GRDB's WAL setup reset `synchronous` after `prepareDatabase`. The actual writer now sets and checks FULL after pool initialization; reader-only assertions were replaced with writer-connection checks before/after commit and reopen. Seven targeted real-file/fence/origin tests passed (`root-writer-review-repaired.log`).
- Root Edge rerun of the exact sanitized command in `supabase/functions/tests/README.md`: 59 passed, including 23 native PostgreSQL/PostgREST/HTTP steps, zero failures, 5 s (`root-edge-projection-final.log`). Includes additive 040000 durable archive-to-projection replay. Maxwell independently reran 59/23 with zero failures and closed the bounded P1-4 repair (`PROJECTION-040000-INDEPENDENT-REVIEW.md`). No actual B2 or deployment is involved.
- `root-mac-account-history-build.log`: ad-hoc signed, hermetic macOS build-for-testing succeeded. Products were copied unchanged to `/private/tmp/nara-app-tests.NUgt6a` because external-SSD dynamic loading/debug symbol lookup stalled earlier attempts. These are test binaries, not a physical-device Release build.
- `root-account-history-focused.log/.xcresult`: 98 actual app-host tests passed, zero failures, 3.693 s. Covers BackfillActor (11), repaired watchdog (8), account caffeine/log isolation, real Repository single-flight revocation barriers, server-owned null/empty results, real-file scoring-input journal, initial upload queue, raw manifests, snapshot decoding and widgets. Later upload/input source changes require rerun.
- `root-ai-account-focused.log/.xcresult`: 70 actual app-host tests passed, zero failures, 0.839 s: 45 AI/privacy/provider regressions, 19 IMU tests and six capture/account binding tests. Synthetic keychain/network fixtures are used; real credentials or provider requests were not exercised. The separate app-module harness failed compilation due duplicate imported/source declarations; its failed log is retained and not counted as acceptance.
- New durable account-scoped `history-inputs.sqlite` retains profile/configuration/sleep-edit inputs, expected revisions, retries and conflicts. Four native real-file tests passed both standalone and in the app-host run. Server-owned sleep edit/delete/undo/manual-nap integration is being tested; controls remain gated until that contract is verified. It never calls local restaging for owned sessions.
- Account runtime now scopes log tails, debug exports, caffeine, AI preferences/keys/sessions and writer lifetimes. Repository overlays are presentation-only: authoritative nulls remove old values, complete empty sleep replaces the prior set, and rollback restores retained local source rows. HealthKit/widget/watch consumers use the same presentation state. Physical HealthKit/watch/device behavior is unexecuted.
- `9477a2f`: reviewed local writer durability, account ownership, receipt ledgers, unknown-store preservation, PPG schema identity and shared schema fixtures. `e19be7b`: reviewed Edge intake/durability/projection replay plus additive 020000/040000 migrations. No deployment.
- `root-input-repaired-build.log` and `root-input-lifecycle-repaired.log/.xcresult`: current hermetic app build succeeds; **109 actual app-host tests passed, zero failures, 2.040 s**. Includes real account construction/analysis retirement barriers, scoped baseline recalibration, typed input journal/RPC/coordinator, receipt integration, history/DTO/content-ready state and five sleep-input tests. Sleep edits preserve snapshot device/identity/timezone and earliest affected wake day, never restage server-owned sessions locally. This run predates optional context-consent integration.
- W5 repair handoff: 41 actual-source native wrapper/transport/journal tests and 54 NoopPush tests passed. Exact account/receiver binding, immutable staging versus verified keys, complete typed receipts and crash-safe source/cursor/cleanup settlement are implemented; root's app-host run above includes receipt regressions. OS background-daemon restoration is not reproduced by controlled transports. Continuous IMU membership integration remains active.
- W4 input checkpoint: 76 service passes plus 741 kernel passes (five existing skips), installDist successful, recorded in `W4-INPUT-CONTRACT-CHECKPOINT.md`. Additive 050000 introduces immutable ten-argument profile/config/sleep/context inputs, stable anchored sleep identities, CAS/head/as-of RPCs and replay groundwork. This is NOT complete historical scoring or metric parity. Fresh-install head handling and explicit conflict resolution remain client integration work.
- Android handoff: APK compilation succeeded; focused 210 tests passed. Full 5,628-test invocation had 5,616 passes, six failures and six skips, so the full suite is NOT green. Three schema expectations have since been repaired in shared fixtures but need an Android rerun; the HRV/RecoveryDrivers failures require attribution or repair. W4 outbox and same-account retired-writer fencing are under implementation.
- Open local work: complete ordered historical computation and visible metric replacement; unknown/deep quarantine archive and collector shutdown; auxiliary same-second identity and missing scalar/continuous IMU lanes; typed input HTTP integration/conflict resolution; optional account-scoped context consent/producers; Android and independent integrated reviews. These are repository implementation tasks, not external acceptance blockers.

## Requirement ledger

### Populated context and Android retirement findings, 2026-09-18 08:39 local

- `c5ab215` checkpoints receiver060/070/080 and their reviewed native harness. Independent source review also closed the two-file JVM reader-mode correction (`ROOT-W4-READER-MODE-INDEPENDENT-REVIEW.md`). The13-case v2 pass is now historical: five new Swift APIs and two test-only provenance changes intentionally make its old source manifest stale. Current-source v3 qualification remains required; the JVM manifest gate is not weakened.
- A fresh64-observed-day synthetic producer passes **1 test, zero failures/errors/skips,26.302s; build41s**, using real raw reads,68 ordered claims, fenced snapshot publication and authenticated RPC readback. It exports `populated-context-interop.fqb3ZL/W4-POPULATED-CONTEXT-SNAPSHOT-V2-NATIVE-FIXTURE.json`,366184bytes,SHA256 `fbc6b5d269979aced162916f937a4be52520cf17c218b23bef3d6cb7907946ed`. Its first run failed an illness respiratory-baseline eligibility assertion; the preserved repair changes only synthetic RR timing to beat-accurate RSA construction and leaves all assertions intact. See `W4-POPULATED-CONTEXT-INTEROP-PRODUCER-HANDOFF.md`.
- Root compiled the actual Swift DTOs/adapters and seven dedicated tests against that exact populated fixture: **7 passed, zero failures/skips,0.312s** (`root-populated-context-native.log`, reproducible `Tests/ServerScoreContextNative/run.sh`). Explicit prior-build analytics types are linked; no engine computation or actual AppModel is substituted. Fresh integrated app testing still waits source freezes. The fixture remains partial for other documented input gaps. Adding the missing distance-result fields is a following implementation slice, not yet acceptance.
- Historical-corpus provenance now has a test-only source candidate that verifies215/217 exact original source hashes against immutable export checkpoint Git blobs while retaining every live Swift output-byte comparison. **5 standalone helper tests pass, zero failures/skips,34.676s**, including both complete actual Git inventories; full export suites and independent review remain pending. Neither original corpus/manifest/expected result is rewritten. See `ROOT-HISTORICAL-CORPUS-PROVENANCE.md`.
- Android W2 independent review confirms six P1 source defects despite the earlier5751-test/zero-failure checkpoint: generic live retirement drain, interrupted GPS recovery, independent failed IMU-tail drain, continuous-IMU upload binding, stale/global backup restore, and unfenced calibration/custom-zone preference setters. `ANDROID-W2-INDEPENDENT-REVIEW.md` pins all130 unchanged source hashes and exact call chains. Rawls owns restore/profile repairs; root owns durable GPS recovery; Banach is preparing captured generic/IMU drain integration after the separate pure receipt parser. No affected item is marked independently closed and no phone database restore/reset is authorized.

### Receiver checkpoint and immutable-v2 parity, 2026-09-18 08:26 local

- Local commit `2b017ec` records the independently closed operational evidence gates. The following receiver checkpoint contains the23 exactly reviewed Edge/migration paths, with the two root coverage-repair hashes substituted into `EDGE-060-070-080-INDEPENDENT-CLOSURE-REVIEW.sha256`. All23 combined hashes match. Independent review closes the auxiliary P2 after **3 tests/13 real PG/PostgREST/HTTP steps, zero failures,4s** (`ROOT-AUX-COVERAGE-REPAIR-INDEPENDENT-CLOSURE.md`); root's full72/55/0 result is recorded above. No P0/P1 remained in the bounded receiver review.
- Additive060/070/080 preserve scalar projections and provenance, reject divergent same-key values without discarding independently archived alternatives, validate auxiliary identities before atomic receipt/index publication, retain unsupported typed-validation debt, and accept explicit PPG input-selection metadata. The old deployed migration history is unchanged. Retain these additive tables/provenance/receipt constraints during a compatible rollback; do not drop pending archives/debt to revert binaries. Protocol1.4 remains unadvertised pending actual paired input-selection sender qualification. The synthetic object HTTP server does not prove real B2 signature/lifecycle behavior.
- Root's two-mode reader repair now passes **23 focused tests, zero failures/errors/skips**, and the separate immutable-v2 task passes **all13 actual-Swift kernel-calendar cases** (oneJUnit wrapper,12.337s; combined build19s). `root-w4-reader-mode-after-v2.log` and both archived XML directories preserve the evidence. All217 current Swift hashes and every corpus digest were checked before the next five additive Swift APIs were released. No expected output/tolerance was changed. Independent runner-delta review remains active; full server-day/DST/context acceptance remains open.

### Operational closure and current regression gates, 2026-09-18 08:24 local

- The operational checkpoint now rejects failed/malformed inspections, binds the independently reviewed exact container/image/canary identities, checks every trace and heartbeat, and accepts the checked-in migration runner's exact supported full-basename ledger without rewriting it. Raw ledger entries are preserved beside canonical IDs. Root reran **70 offline tests, zero failures/skips,32.849s** (`root-ops4-contract-closure-offline.log`); independent review separately reran70/0 and closed both deployment-contract P1 findings (`OPS4-CONTRACT-CLOSURE-INDEPENDENT-REVIEW.md`). All19 review hashes match, including seven import-only JVM dependencies left for their own scoring checkpoint. This focused commit includes only the12 ops/runbook paths plus this ledger. Real documented `--local` native execution and actual immutable-image provenance remain separate gates; no remote/configuration/credential access occurred.
- The stored RR channel correction passes **17 native PG-backed tests, zero failures/errors/skips**, preserving the historical query oracle and all217 current Swift source hashes. The next immutable-v2 run still fails the ninth case, now on an extra step-counter predecessor: eight pass/four unreached (`root-w4-v2-stored-rr-whole-day-parity.log`). The corpus's `kernel_calendar` mode uses ordinary bounded reads, while the JVM harness incorrectly chose historical predecessor loading for both modes. Root reproduced this in a two-mode native test before changing only the runner's mode dispatch. Historical/server-day predecessor admission is preserved; corpus bytes/comparator/tolerances are untouched. The repaired runner and exact v2 gate are running, not yet claimed green.
- Warm-reset repair is now independently closed: separately compiled probe yields warm10/cold10 with exact state equality. Root reran **100 Swift tests, zero failures/skips,2.358s** (`root-swift-history-warm-reset-repaired.log`). Trusted composer-internal observation and sleep-attribution boundaries remain explicit; this is not full raw-to-server-day acceptance.
- Edge independent review ran72/54 with no P0/P1 and found a P2 auxiliary coverage overstatement. Root's added real-PG regression first failed, then the full repaired receiver suite passed **72 tests/55 steps, zero failures,14s** (`root-aux-coverage-before.log`, `root-edge-aux-coverage-after.log`). New, undeployed070 now withholds expected/missing/coverage for identity-v2 auxiliary rather than treating sibling count as elapsed-second occupancy; exact bytes/received count and legacy-entrypoint recovery remain tested. Old020 is unchanged. Independent two-file closure is pending before receiver checkpoint.
- Account HRV source review found no P0/P1 in the pinned six-file delta;12 pure-facade tests passed. Fresh actual-app verification remains pending prepared-selection source freeze. Populated context producer/consumer tests are compiled/parsed but not executed; their output must be generated in a fresh evidence directory and must not borrow the prior null-context fixture.

### Historical schema checkpoint and newly exposed gates, 2026-09-18 08:05 local

- `98d2a94` preserves the scoring-input client checkpoint. Additive `20260918050000_production_scoring_history.sql` is now checkpointed separately before its receiver dependencies. Its SHA-256 remains `82094fb6128917878e4a613872e3664339c1fdaa0775dde25d7def9bb872bbf1`, identical to the reviewed W4 freeze. It adds immutable effective-dated inputs, owner-checked CAS/head/as-of RPCs, ordered historical generations/checkpoints, stale-publication fences and conservative dependency invalidation. Existing migration files are unchanged. No schema was applied outside disposable synthetic clusters. Retain this additive schema during a compatible binary rollback; do not drop input/debt/checkpoint history to downgrade.
- Root's previous **232 service tests / 1,010 kernel passes plus five existing optional skips / installDist** remain the full JVM checkpoint (`root-w4-context-orchestration-full-native.log`). The current full Edge rerun passes **72 tests, 54 steps, zero failures, 12 s** (`root-edge-current-full.log`), with the documented sanitized, loopback-only command and actual migration chain through080. Existing integration-test object-server code was extracted intact to `local_objects.ts`; no integration assertion was removed. These checks are not deployed RLS/B2 or release acceptance.
- After the indexed identity lookup and materialized RR query repair, the dedicated immutable-v2 parity gate completes in **25 s and FAILS** (`root-w4-v2-indexed-rr-whole-day-parity.log`, archived XML directory alongside). Eight kernel-calendar cases pass; the ninth, `whoop4-legacy-boundaries`, selects extra `rr-10` on JVM. Four later cases are not reached. The raw fixture labels this row channel2; Swift excludes the persisted `RRSourceChannel.spo2Ibi` value2 while the current server query excludes110. Full producer/receiver lineage is under review before a policy change; expected values and tolerances remain untouched. This is not full server-day/DST acceptance.
- Independent Swift-history review reproduced a warm-checkpoint/reset-gap mismatch: with a Jan1 checkpoint, Jan11 HRV reset and Jan21 observation, warm state ages19 missing nights while cold state ages10. The analogous JVM loop requires a native regression. Mill owns the bounded repair. Coherently tampered historical before-state and arbitrary old-sleep attribution are separately documented trusted-input boundaries, not claimed validation guarantees.
- Operational source/native repair passes54 synthetic tests, but independent closure found two remaining deployment-contract mismatches: the checked-in migration runner stores full filenames, and Compose service `scoring` has no fixed container name. Banach owns strict supported-ledger normalization and an independently bound exact-container selector; no real SSH or configuration/secret access is authorized. See `OPS4-REPAIR-INDEPENDENT-CLOSURE-REVIEW.md`.
- Root's account-HRV preference follow-up passes **12 pure-facade tests, zero failures, 0.021 s** and syntax parsing. Editor, local pass/fingerprint, server configuration and Today explanation use the same captured account value; legacy globals remain untouched and are not adopted. Fresh actual-AppModel execution and independent review remain pending. The new local cycle bindings likewise still require the next integrated app build.

### Scoring-input checkpoint, 2026-09-18 07:50 local

- `3c208ca` preserves the typed readback checkpoint. The following focused input checkpoint captures the separately durable account-owned journal, revision/head/receipt protocol, bounded conflict retention, consent denial barrier and control reserve, owner-fenced coordinator, bounded RPC readback and native/real-loopback harness. Exact allowlist: `ROOT-SCORING-INPUT-CHECKPOINT.files` in the evidence directory.
- Independent reserve review ran **60 tests, zero failures, 22.630 s**, including the full 4096-row/16 MiB held-queue case (`CONSENT-CONTROL-RESERVE-INDEPENDENT-REVIEW.md`). Root's subsequent completion-callback regression reproduced two new assertion failures before repair and passed **61 tests, zero failures, 23.779 s** afterward (`root-consent-completion-before.log` / `root-consent-completion-after.log`). Bacon independently reviewed that exact three-file delta and hashes in `ROOT-CONTEXT-PUBLICATION-INDEPENDENT-REVIEW.md`. The callback now runs once after saving clears, including failed writes, and never after retirement.
- The current 152-test app-host run includes the consent/journal/coordinator/capacity cases; the separate 122-test app-host run includes typed input readback/streaming transport. Earlier seven real Swift-to-PostgREST tests exercised ordinary authenticated roles, lost-response replay, opaque revisions, conflicts/rebase, tombstones and cross-owner rejection, with 13 distinct synthetic server mutations. These are local integration evidence, not deployed RLS acceptance.
- Pending debt is not evicted to make room. The reserved denial lane does not guarantee delivery through every pre-existing conflict or disk failure, and local denial cannot recall an already-sent request. App producer preference-to-outbox crash windows and complete consumer/input UI integration remain separate open work. No production data, personal credentials or deployment was touched.
- Root also reproduced the frozen operational repair's **54 offline tests, zero failures/skips, 26.395 s** (`root-ops4-repair-offline.log`) and source scan of 267 Kotlin files/eight additive migrations. Independent review is active; no remote acceptance or whole-day parity pass is inferred.

### Readback checkpoint and current verification, 2026-09-18 07:45 local

- The fresh hermetic macOS app host passed **152 tests, zero failures/skips, 27.565 s** (`root-context-barrier-app-tests.log/.xcresult`), including the six additional real-Repository suspension/retirement controls for context publication. The local illness and cycle enable flags are rechecked after suspended reads; server-owned nulls do not reopen local computation. This build predates the subsequent account-preference and prepared-selection changes, which require their own fresh integrated build.
- Wider app-host readback passed **122 tests, zero failures/skips, 0.584 s** (`root-current-readback-bundled-app-tests.log/.xcresult`, summary JSON alongside). Its exact command is retained at the beginning of the log. The earlier external-volume fixture run timed out at `testActualServerFixtureWorkoutEnvelopeRoundTrips` and remains failed evidence in `root-current-readback-app-tests.log/.xcresult`. Copying the same synthetic JVM fixture into the isolated test-host bundle, updating only the temporary xctestrun fixture path and ad-hoc re-signing with the same entitlements resolved the stall. Both fixture copies have SHA-256 `6a22d6e58ff7d989b8e3a1eefa860db4d49ddf95df5efdc7c07846f009b44952`; no assertion, source, OS permission or production app changed. The exact OS cause is not proven. The fixture has null context details and does not certify populated context interoperability.
- The readback source checkpoint contains strict bounded schema-2 snapshots, sleep/workout/context detail DTOs, typed presentation adapters, account-fenced streaming transport, explicit metric ownership, async bounded cache repository, state-readiness tracing and their native regression harness. Its precise file allowlist is `ROOT-READBACK-CORE-CHECKPOINT.files` in the evidence directory. The 101-test native reader plus four real-settings tests also passed against the generated JVM fixture (`root-current-context-readback.log`). This is not complete W4 consumer migration or device rendering/performance acceptance.
- Local checkpoints `4ad6145` and `8457c72` preserve the reviewed auth/storage foundation and W5 receipt/association/IMU core respectively. The remaining crash window before any receipt association and multipart preparation now have an active immutable prepared-selection implementation. Do not infer that the existing 71-test core closes those new cases.
- Root reran the new test-only Swift history increment: **97 tests, zero failures/skips, 2.190 s**, `root-swift-history-first-increment.log`. Actual helpers exercise 131-day history, restart, correction/reset/source/recipe behavior and sleep need/debt, but inputs are synthetic observations rather than raw-to-result scoring. Independent review is active; no schema-2 full-day corpus has been exported.
- Remaining local work includes the RR query/index performance repairs and exact v2 JVM parity rerun, full Swift/server day composition, account-scoped cycle preferences, populated context/distance mapping, prepared-selection recovery and operational evidence-binding repairs. The prior ten-minute v2 JVM run was stopped without a parity verdict. Device, deployment, B2, 72-hour backlog and overnight gates remain NOT_READY and no external mutation is authorized.

### Versioned Swift fixture checkpoints, 2026-09-18 07:25 local

- Swift checkpoints: `8b7da7c` immutable historical exporter/corpus, `3647bd2` three additive APIs and19 regressions, `b5183a5` corrected v2 native corpus. Root211/0 and the independent API/v2 reviews support these bounded claims; full server-day parity and old current-source gate reconciliation remain open.
- Root verified all17 W5 core hashes plus10 pinned prerequisite hashes. The fresh app-host companion run passes **30/30 in2.161s** (auxiliary identity, IMU bootstrap and continuous recorder), using the same build as the146-test gate. `root-w5-checkpoint-companion-app-tests.log/.xcresult` covers the checkpoint's additional capture/retention adapters. Core durable journal/queue/transport, typed source settlement, exact IMU archive and protocol-namespace recovery are checkpointed together; the separately proposed pre-transport prepared-selection work is not included and remains required. Account runtime/readback integration remains in the worktree, not a whole-branch release claim.
- `fd0f7aa` checkpoints the evidence validator/runbook and prior integration ledger only. Subsequent broader ops review found two fail-closed/correlation defects and two measurement-series/freshness gaps; these are being repaired with offline synthetic runner tests. The bounded eight-migration check remains verified; no broad readiness claim follows from that commit.
- The original three-file actual-Swift exporter and14 immutable v1 corpus files are retained as historical evidence, including the diagnosed synthetic raw1/state1 contradiction. This is not a passing JVM parity corpus. Root's211-test run includes byte-for-byte actual Swift replay of all13 historical default results, while separate old current-source hash gates intentionally remain stale at the three additive APIs. The versioned v2 corpus corrects the synthetic input before native execution and preserves every expected result.
- The dedicated v2 JVM gate was deliberately stopped with TERM to its verified test-worker PID after **10m46s** (exit143, `root-w4-v2-whole-day-jvm-parity.log`), not reported as a test pass or semantic mismatch. Captured thread evidence shows quadratic test-only identity lookup over28,800 rows; a separate canonical-RR query also consumed substantial CPU. Root is repairing the lookup with linear, exact-identity indexing and negative controls; the production query is independently under review. Frozen expected values, comparison tolerances and source selection policy are unchanged.
- Root's latest standalone readback run passes101 tests plus4 settings tests and decodes/round-trips the current actual JVM-produced synthetic snapshot (`root-current-context-readback.log`). This does not establish all-field visible consumer acceptance.

### Integrated native follow-up, 2026-09-18 07:18 local

- Root's fresh macOS app build succeeds; actual app-host execution passes **146 tests, zero failures/skips, 28.170 seconds** (`root-context-publication-app-tests.log/.xcresult`). This includes runtime replacement8, receipt28, queue30, IMU13, generic capture12, retained drain3, consent capacity19/relay10, context adapters5/app publication7, W1 account isolation5 and IMU retirement2. The runtime constructor-race repair immediately publishes an ownerless presentation while retaining the original writer. Separate iOS conditional-path and physical lifecycle acceptance remain open.
- Root reproduced the consent completion-order defect with a new native regression (61 tests, two assertions fail only in that new test), then moved the completion callback after `saving=false`, preserving synchronous pause and suppressing retired callbacks. The native rerun passes **61/61 in23.779s**; app tests additionally verify immediate cached grants and restoration of an unaffected purpose after journal withdrawal. Logs: `root-consent-completion-before.log` and `root-consent-completion-after.log`. Independent review of the callback delta remains active. A separately found stale local feature-toggle continuation is being repaired; the146-test result predates that follow-up.
- Root's complete Android rerun (`assembleFullDebug`, `testFullDebugUnitTest`, `--rerun-tasks`) succeeds in **3m39s**:702 suites,5,751 tests,5,745 passes,six existing skips,zero failures/errors. This includes the two real paired-v2 Room upgrade/retry/receipt-retirement controls. Log: `root-android-paired-v2-full.log`. This is host/APK evidence, not device or server-backed migration acceptance.
- Root's actual Swift replay/seam/regression gate passes **211/211 in62.604s** (`root-swift-v2-and-seams-native.log`). The new v2 kernel corpus corrects1,560 synthetic raw band-state bytes before native execution; all13 expected outputs remain unchanged, and immutable v1 evidence is preserved. Current217 source hashes and16 v2 freeze hashes verify. The dedicated JVM v2 gate remains running; a slow canonical-RR query and quadratic test-only identity lookup are under investigation. No full server-day or whole-package green claim is made.
- W5's two independent pre-stage crash regressions are now permanent, bringing the native source/receipt/queue suite to **71/71** (Rawls execution4.807s); root's146 app gate includes all71. `ROOT-W5-PRESTAGE-CONTINUATION-INDEPENDENT-REVIEW.md` closes the bounded after-association/before-stage fix. The earlier queue-receipt/before-association window, complete multipart continuation and ambiguous legacy maintenance remain open.
- The evidence validator requires all eight additive migration IDs, a unique string ledger, and the previously required artifact/identity/runtime evidence. Root and independent native runs each pass **9/9**; missing-manifest preflight rejects before secrets/remote access. `ROOT-OPS-EIGHT-MIGRATIONS-INDEPENDENT-REVIEW.md` closes this bounded membership check. Broader local acceptance-script import scanning and deployment-environment isolation are being reviewed separately. This checkpoint is neither live-ledger verification nor release approval.
- No push, merge, deployment, production write, phone installation/reset/restore or physical-device acceptance was performed. Remaining repository work is not reclassified as an external blocker.

### Recovery and independent-review follow-up, 2026-09-18 06:50 local

- `f7f5cd6a109dd924e146c36547807fd878ff90e6` checkpoints only reviewed Swift PPG contributor-selection provenance and this ledger. The integration worktree still contains separately owned implementation slices; it is not a release candidate.
- Root's current actual-source W5 native harness passes **69 tests, zero failures,4.615 seconds** (`root-progress-prestage-full-native.log`):26 receipt,13 IMU and30 upload queue tests. Historical protocol namespaces recover staged cleanup before new selection; new binary receipt associations atomically include the exact cleanup continuation, closing the independently reproduced after-association/before-stage gap across1.2/1.3 to1.4 with network unavailable. Settlement reclaims exact association files and retains debt on partial unlink failure. The first new-test run failed on Foundation relative/base URL equality; exact scanned filename validation repaired it, then focused and full reruns passed. Independent review of this delta remains pending. The distinct pre-association queue-receipt window, multipart inline continuation and ambiguous legacy-association maintenance remain open.
- Root's reserved consent capacity gate remains60/0, and a fresh authenticated local PostgreSQL/PostgREST run passes7/0 with13 mutations (`root-consent-control-loopback.log`). Bacon independently reran60/0 and found no new confirmed P1; same-entity conflict still requires explicit resolution. Banach reports full Android5749 tests/zero failures/six existing skips and APK assembly, but root's combined rerun and valid paired-store upgrade extension remain pending; prior focused/native claims are not substituted for those gates.
- Rawls reproduced the initial AccountAppRuntime construction race before repair (two new cases failed18 assertions). The two-file repair synchronously retires the rejected owner, publishes only a neutral model and retains the old captured writer until its drain succeeds. Seven actual-runtime native tests pass; root inspected source and tests, but fresh app-host/iOS compilation remains required. Generic journal fixtures now explicitly close all four old/reopened SQLite handles before deleting their synthetic directory; independent8/0 native, no production generic changes in that teardown repair.
- Mill's three historical-admission P1 repairs are frozen for independent review:225 service passes;1010 kernel passes/five existing skips; installDist succeeds. Strict provenance actual PostgreSQL cases pass4/4 after supplying the missing required fields in the valid fixture. Root has not yet rerun this new freeze. Whole-day Swift/JVM parity still fails: after preserving null waist and native workout zone0 semantics, strict intake rejects synthetic `state:1/rawByte:1` band-state rows. The old corpus is retained as failure evidence; exporter correction requires a new actual-Swift versioned corpus, not rewritten expected values.
- Pure Swift context adapters now require emitted engine notes/copy/labels and independent metric ownership; optional DTO fields preserve old cache decoding. These new adapters/tests are not yet app-compiled, and existing AppModel context producers are not yet cut over. New layout work remains paused for the outstanding antislop usage-mode choice; backend/data-contract work continues.
- The repository-side evidence validator now requires all eight additive production-sync migration IDs and rejects string/duplicate/malformed ledgers. Nine synthetic validator tests pass (`root-ops-eight-migration-validator.log`); this is validator coverage only. No deployment, live canary, physical-device, energy, overnight or72-hour acceptance has been run.

### PPG selection metadata checkpoint and integration follow-up, 2026-09-18 06:20 local

- The focused Swift package checkpoint adds optional derived-only input selection without changing either existing PPG estimator. Unknown, null and non-derived selection reject; legacy absence stays unspecified. Duplicate-timestamp input framing retains record boundaries and encounter order. Root's protocol748/store607/sender69 gates and Rawls's `INPUT-SELECTION-METADATA-INDEPENDENT-REVIEW.md` cover this bounded change; receiver080, Android producer wiring and JVM admission are separate integration changes, not deployment or activation.
- Root reran the full consent native harness on the reserved-control repair: **60 tests, zero failures,25.637 seconds**, `root-consent-control-native.log`. The original fully-held4096-row and16MiB limits now have retained-debt/denial regressions; independent reserve review and Android parity remain in progress.
- Actual Swift kernel corpus now contains13 cases from unchanged Swift algorithms, with215 recorded source hashes. Root's first strict JVM gate failed honestly on `profile.waistCm` absence (`root-w4-kernel-13-initial.log`,2 seconds). The Swift model has no waist field; adapter repair must preserve that absence and use Kotlin's existing unset representation, never manufacture a measurement. No whole-day parity pass is claimed.
- Independent native orchestration review reproduced three P1s: calendar/reset-ineligible history feeding derived readiness/weekly/stress outputs, no-data cycle classification becoming confident follicular, and pre-reset cycle features re-entering the classifier. `W4-ORCHESTRATION-INDEPENDENT-REVIEW.md` retains exact evidence. Mill owns fixes; the earlier197-test green does not close them.
- Mill's strict provenance repair passes11 unit tests but exposed an incomplete supposedly-valid integration fixture. That fixture is being made contract-complete, retaining its assertions and future-window rejection. Rawls independently closed the IMU retirement finding and repaired W1 fixture teardown (7 native passes); Mill closed both sleep findings in source, with root's74-test app run passing apart from one external-fixture skip. Full new integrated app verification remains open.
- Generic capture now uses an independently opened, owner-validated old-account writer and is joined alongside BLE during retirement; helper tests pass8/8, but real runtime replacement and app-host composition still require verification. W5 review also identified cross-protocol cleanup debt stranding and lifetime receipt-association accumulation; repair and tests are in progress. No missing device or deployment evidence has been converted to a pass.

### Integration review and recovery follow-up, 2026-09-18 06:00 local

- Local checkpoints now include `d04a374d71a33817ce88f023934e36bd4b249a80` (strict capture provenance/auxiliary identity) and `b5fbd4eb0cbd5c2411fad75724c8e24ba03efe79` (negotiated sender lanes). No remote writes or deployment occurred.
- Root reran the frozen scorer: 197 service tests passed, 1015 kernel tests with 1010 passes/five existing optional skips, and installDist passed in33 seconds (`root-w4-freeze-full-native.log`). Nested sleep-stage explicit-null rejection was repaired in NEW050 with authenticated local PostgreSQL regression evidence. Actual whole-day Swift/JVM parity and final orchestration review remain open.
- PPG input selection differs in existing Swift/Android algorithms. Preserve both; optional derived-only `inputSelection` identifies last-record-per-second versus concatenated encounter-order records. Absent metadata stays unspecified. NEW080 extends the PostgreSQL validator without editing070; protocol1.4 advertisement remains disabled. Root gates: protocol748/zero failures/two skips; store607/zero failures/one skip; sender69/zero failures; Edge72 tests/54 steps/zero failures through the additive080 chain. Logs: `root-ppg-selection-{protocol,store,sender}.log`, `root-edge-080-full.log`. JVM validator alignment and Android actual-producer evidence remain in progress.
- Root consent native54 and actual local PostgreSQL/PostgREST7 pass (`root-consent-capacity-{native,loopback}.log`,13 distinct mutations). Independent review nevertheless reproduced a fully consent-held4096-row queue that cannot admit its denial; reserved input control capacity is being repaired. Existing passing tests do not close that case. The earlier consent pause/reopen and compaction repairs passed bounded independent review.
- Independent account-retirement review found two P1 gaps: generic-strap fire-and-forget persistence and unflushed IMU file tails. Root IMU repair checks both captured session/continuous stores, off-main, across every pending device/window; two actual BLE/file app tests pass. Generic-source repair and end-to-end replacement testing are still open. Do not call the account-wide retirement boundary complete.
- Fresh app run `root-retirement-final-tests.xcresult` is NOT GREEN:156 total,154 passed,one external-fixture skip,one test-process signal kill. The affected MissingImuBinding test and preceding lost-completion test passed in an isolated two-test rerun (`root-receipt-kill-isolated.xcresult`); cause is not established. Consent fixture unlink warnings are gone on that run; W1 account-isolation fixture SQLite teardown warnings still need repair. Keep the failed artifact and repeat the integrated gate.
- Typed readback review found per-result sleep capability withdrawal and main-night/nap warning-selection defects; root fixes are in verification. Updated native readback101 plus settings4 pass (`root-readback-withdrawal-workout.log`). Its real server-generated synthetic workout set is empty: this proves envelope compatibility, not populated workout interoperability. App sleep-model regressions and populated server fixtures remain separate gates.
- CurrentHRV's inherited Android coverage expectation was checked against actual unchanged Swift execution:1.017391304347826,23 clean beats,RMSSD0 for the ectopic vector. The old0.8 assertion is not the Swift oracle. Evidence: `/Volumes/Untitled/whoop-w1-evidence-20260918-42qzUs/CURRENT-HRV-NATIVE-ORACLE.md`; Android correction/rerun pending, no physiological formula change authorized.

### Reviewed W1 package checkpoint and app follow-up, 2026-09-18 05:30 local

- Local commit `d04a374` contains the reviewed protocol/store checkpoint below. No push or deployment.
- Focused checkpoint scope is the 21 frozen W1 protocol/store files listed in `W1-FINAL-EXCLUSIVE-CHECKPOINT.md`, additive v51/v52 registration, both byte-identical Room-42 schema oracles and this ledger. All 34 W1 source hashes were verified; the 13 app/BLE files remain a separate integration checkpoint. No previous migration body changed.
- Root defensive review found and repaired two contract gaps: explicit-null/non-JSON-safe provenance could enter the local model despite wire rejection; invalid persisted quarantine trim could trap conversion. The model now rejects known null members and unsafe integers. Quarantine read/replay/archive refuse invalid trim and retention preserves the original evidence. New native regressions demonstrated failure before the repair; no formula/golden or acceptance assertion was weakened.
- Root exact commands use `env -i PATH=/usr/bin:/bin:/opt/homebrew/bin DEVELOPER_DIR='/Volumes/External SSD/Xcode.app/Contents/Developer' TMPDIR=/Volumes/Untitled/nara-production-sync-evidence-20260918/scoring-input-repair-6WYhJW xcrun swift test --package-path Packages/WhoopStore --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/root-store-build --jobs 4` and the same command with `Packages/WhoopProtocol` / `root-protocol-build`. Results: Store **607 tests, zero failures, one existing device-copy skip, 5.349 seconds** (`root-w1-defensive-store.log`); Protocol **746 tests, zero failures, two existing skips, 0.453 seconds** (`root-w1-defensive-protocol.log`). Maxwell's first Store run had an unchanged concurrent-open SQLITE_BUSY failure; its log is retained, not removed from the record.
- Independent reviewer Rawls inspected the exact frozen keying, rowid/BLOB preservation, quarantine/receipt, provenance and migration sources plus both root native logs. No additional P0/P1 or blocker was found for this bounded package checkpoint. This does not close device/low-disk/72-hour or complete W1 acceptance.
- Fresh hermetic macOS app build passed (`root-w1-capture-recovery-build.log`). Selected app-host run **122 tests, zero failures, 8.648 seconds** (`root-w1-capture-recovery-tests.log/.xcresult`) includes 4 exact auxiliary receipt tests, 34 IMU/receipt tests, 30 upload queue tests, 11 capture tests, 8 watchdog tests, 4 runtime construction tests, 3 retired-drain tests, 7 sleep-detail tests and 21 in-progress consent tests. The earlier 81-test run failed three assertions because the new unknown-identity fixture contained no auxiliary payload; adding a real payload field preserved the original three-sibling assertions. No production record-handling change was made for that fixture failure.
- The 122-test run still emitted SQLite live-file-unlink warnings from the new consent-capacity fixture teardown. Pauli owns that repair; consent capacity and its independent review remain open despite passing assertions. The W5 fixture teardown did not emit those warnings.
- Retired capture now stays pinned to its old writer until durable success; further account models allow presentation/readback but do not start WHOOP or generic capture while that drain is pending. This bounds capture-enabled retired generations without evicting accepted bytes. Rebuilding capture after successful drain and the runtime ownership review remain separate from physical OS termination/power-loss evidence.
- Rawls independently ran 34 actual-source IMU/receipt tests, zero failures, and inspected exact membership, source commit, sidecar and cleanup wiring (`W5-EXACT-CLEANUP-AUX-INDEPENDENT-REVIEW.md`). No additional P0/P1 found in that bounded review. Full worker protocol upgrade/deferred-cleanup coverage and real background-daemon/device gates remain open.

### Reviewed sender 1.4 checkpoint, 2026-09-18 05:35 local

- NoopPush adds optional-u32 auxiliary identity in format 2, exact same-second fingerprints, compatible PPG framing at 1.4, schema-2 scalar provenance and real step/sleep-state/PPG-HR append lanes at 1.1 and later. Known provenance/identity cannot be stripped for older receivers. Unsupported rows stop the contiguous prefix; source receipt association still precedes cursor settlement. Numeric ACK fields reject Boolean/string/fraction/overflow instead of trapping or coercing.
- Root reran the clean-environment NoopPush command above: **68 tests, zero failures, 2.882 seconds**, `root-sender-14-checkpoint.log`. Added one cross-platform vector/export test, then reran with `NOOP_EXPORT_SCALAR_FIXTURE=/Volumes/Untitled/nara-production-sync-evidence-20260918/scalar14-swift`: **69 tests, zero failures, 2.832 seconds**, `root-sender-14-goldens.log`.
- All three actual Swift scalar NDJSON bodies are byte-identical to Banach's actual Android exports (`cmp scalar14-swift/<stream>.ndjson scalar14-android/<stream>.ndjson` for stepSample, sleepStateSample and ppgHrSample). This covers synthetic known metadata serialization, not automatic Android PPG estimator provenance. Actual auxiliary decoded bytes, manifest and logical identities match across Swift/Android; gzip differs only in its platform OS-header byte, with each wire digest checked independently.
- Rawls's bounded source review covers the changed coordinator, codec, capabilities, schema mapping and typed receipt matching; recorded production hashes still match. The new app-host auxiliary receipt/progress tests pass 4/4. The sole changed hash since that review is the corrected no-payload test fixture, not production source. Receiver support exists, but 1.4 advertisement remains disabled pending complete receiver/scorer/producer activation gates.

### Auxiliary identity, scalar lanes and integration review, 2026-09-18 05:18 local

- Root registered additive `v51-v18-aux-record-identity` and `v52-scalar-provenance`; both shared schema oracles now pin Room 42. The identity helper preserves exact old rowids/BLOBs and timestamp receipt keys, distinguishes unknown/zero/same-second indexes, and uses the same named unique compatibility-key index as Room. Scalar columns are nullable with no backfilled provenance. Earlier deployed migration bodies are unchanged.
- Root `env -i PATH=/usr/bin:/bin:/opt/homebrew/bin DEVELOPER_DIR='/Volumes/External SSD/Xcode.app/Contents/Developer' TMPDIR=/Volumes/Untitled/nara-production-sync-evidence-20260918/scoring-input-repair-6WYhJW xcrun swift test --package-path Packages/WhoopStore --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/root-store-build --jobs 4 --filter 'V18AuxIdentity|SchemaOracle'`: **22 passed, zero failures**, 0.295 seconds (`root-v51-v52-integrated.log`). Full command without `--filter`: **606 tests, zero failures, one existing device-copy skip**, 5.107 seconds (`root-v51-v52-full-store.log`). Follow-up defensive provenance/legacy-trim checks are in review and require another exact-source run.
- Native sender suite with the same clean environment, `--package-path Packages/NoopPush --scratch-path /Volumes/Untitled/nara-production-sync-evidence-20260918/root-push-build --jobs 4`: **68 tests, zero failures**, 2.959 seconds (`root-aux14-intake-fixture.log`). Tests cover negotiated 1.4 auxiliary identity and schema-2 scalar provenance, legacy-prefix refusal, exact receipt-before-cursor ordering, byte-stable retry, JSON numeric validation and separate old/new auxiliary progress. The last progress/source-membership app tests still await a successful integrated app build; the native package run does not cover them.
- Actual Swift auxiliary fixtures are `aux14-swift/` (small timestamp codec golden) and `aux14-swift-intake-v1/` (plausible timestamp 1800000000, complete receiver intake). The latter was generated by setting `NOOP_EXPORT_AUX_INTAKE_FIXTURE` to that fresh evidence path for the native command; original fixture bytes were not rewritten to bypass the receiver's plausible-time floor.
- Root reran the exact sanitized loopback-only command in `supabase/functions/tests/README.md`: **70 tests, 54 native integration steps, zero failures, 10 seconds**, `root-edge-070-full.log`. The actual Swift auxiliary bytes pass unchanged through migrations 010/020/030/040/050/060/070, object HTTP and real PostgreSQL/PostgREST, including failed index rollback and server-only repair (`edge-pg-1b2ea09b153e84c/swift-auxiliary-070-proof.json`). Both actual Swift session/continuous `imf1` exports also pass unchanged. This proves immutable archive/receipt/index behavior, not physiological processing, local prune completion, deployed B2 or device acceptance. Receiver 1.4 advertisement remains disabled pending all cross-stack gates.
- Root's earlier selected app-host integration passed **302 tests, zero failures**, 11.553 seconds (`root-consent-relay-integrated.log`), native input tests **32/0**, and real Swift/PostgREST loopback **7/0**. Independent capacity review nevertheless reproduced two P1 consent defects: ordinary consent history limits prevented a restart-durable denial; full input admission prevented the drain needed to free queue capacity. Pauli owns their repairs and new capacity/interleaving tests. Prior green results do not close these findings.
- Root review also identified failed final capture drain ownership at account replacement. `RetiredCaptureDrain` now retains the captured old BLE writer, retries bounded fair passes and releases it only after reported durable success. It never substitutes new account credentials. This repair and its three lifetime/concurrency tests are **not yet accepted**; integrated app tests and independent review remain.
- Latest app attempts exposed a consumer closure compile error (repaired), newly added consumer files missing from the generated project (regenerated), and an in-progress consent repair compile error. Failed logs `root-aux14-app-build.log`, `root-aux14-app-build-repaired.log`, and `root-aux14-runtime-build.log` remain evidence. No successful current app build is claimed from these attempts.
- Mill reports 182 service passes and 1,015 kernel tests with five existing optional fixture skips plus installDist. Root has read the source/contract handoffs, but still must independently rerun the final scorer after source freeze. Complete actual Swift selected-input/full-result parity and remaining detail consumers are active repository work, not external acceptance gates.

### Reviewed AI account-isolation checkpoint, 2026-09-18 04:30 local

- Eight scoped AI source/test files have unchanged hashes from Pauli's handoff and Rawls's independent privacy review. Account-qualified atomic provider/key records, captured Custom endpoint configuration, account defaults, owned URLSession cancellation and obsolete-generation reply rejection were inspected again by root. No personal Keychain items or live providers were accessed.
- Root reran all seven AI classes against the current hermetic app host: **45 tests, zero failures**, `root-ai-checkpoint.log/.xcresult`. Build provenance is `root-consent-relay-tests-build.log`; complete AI review is `ScoringInput-AI-independent-review.md`. Actual account construction/shutdown remains integrated in AppModel's following lifecycle checkpoint. This checkpoint alone does not establish complete W2 or production privacy acceptance.
- Root's independent W5 app-host follow-up now passes **78 tests, zero failures, 3.744 s**, including 11 IMU membership/archive, 20 receipt, 30 queue, seven IMU bootstrap, seven consent and three actual account-construction cases (`root-profile-context-imu.log/.xcresult`). Exact Swift archive receiver integration and independent cleanup review still remain open.
- Independent review identified two new P1s despite earlier passing fixtures: failed consent revocation could reopen in a fresh runtime, and context-helper illness could compress sparse dates or ignore baseline resets. Both have active local repairs; neither is closed by an earlier green suite. Root's initial durable consent relay adds a pre-decision denial barrier and immutable dated config intent, atomic origin-to-mutation import, and separate accepted versus explicitly resolved receipt state. Seven new relay cases plus seven consent, 32 input, three construction and 20 AI cases pass in a **69-test** actual app run, 0.449 s (`root-consent-relay-ai.log/.xcresult`). The complete existing native input suite also passes 32/32 in 0.263 s (`root-consent-input-native.log`). Further failure-path tests and independent repair review are required. Total storage failure before a denial barrier is saved cannot be described as durable revocation; remote computation is not instantaneously cancelled while offline.
- Android's latest native checkpoint assembled the APK and ran **5,678 tests: 5,671 passed, one failed, six skipped**. The remaining failure is CurrentHrvTest.midWindowEctopicIsGapAware; schema and RecoveryDrivers assertions now pass. A 250-test focused run passes. `Android-W4-runtime-consent-checkpoint.md` explicitly retains local preference-to-outbox durability, consent delivery, conflict-chain and Sleep consumer gaps. No Android full-green or activation claim is made.

### Integration follow-up, 2026-09-18 04:08 local

- `f2f40c4`: immutable account/session admission and account storage namespace foundation. `d1702b0`: bound durability receipts and PPG wire identity. Root full NoopPush rerun: 54 tests, zero failures, 2.870 s (`root-account-push-final.log`). Later W5 archive scheduling is a following uncommitted delta.
- Consent uses a separate FULL-synchronous account-owned database and captured before-commit retirement fence. Seven actual app-host tests pass. A real SQLite-trigger write failure reproduces failed revocation: subsequent reloads and another-purpose grant cannot reopen the paused purpose; only an explicit successful retry clears it. New decision IDs do not release old held payloads. Existing local cycle choices are not imported as cloud consent. Entry producers/review UI are still incomplete.
- Rawls's final input admission/retirement checkpoint was independently rerun by root: 32 native journal/coordinator tests pass, 0.236 s (`root-input-admission-native.log`); seven actual Swift-to-PostgREST tests pass, 0.409 s (`root-input-admission-loopback.log`), with 13 distinct mutations and ordinary authenticated ownership checks. SQL before-commit rollback and consent revocation while opening are real file-backed tests, not state-only models.
- Root added typed effective-day input readback and a 72 KiB streaming response cap for the three input RPCs, with captured receiver/generation and no redirects, cookies or shared credential storage. Head revision is distinct from selected effective revision; future payloads, wrong identity, malformed/null/tombstone combinations and oversize payloads are refused. Current app-host focused run: **50 tests, zero failures, 0.349 s** (`root-input-read-transport.log/.xcresult`). Current native 32-test rerun also passes. Seven loopback tests now use the production streaming transport and typed reader, zero failures, 0.483 s (`root-input-read-loopback.log`); actual DB evidence is `edge-pg-a91b31386f355232/scoring-input-loopback.json`. These root follow-up changes await independent review.
- Root's cross-language fixture is generated by actual Swift engines and checked by both the Swift app host and server orchestration. Context JVM suite: **10 tests, zero failures/skips**, BUILD SUCCESSFUL in 10 s (`root-context-swift-parity-native.log`). Earlier unchanged-kernel regression: 88 tests pass. `Tests/Fixtures/CONTEXT_PARITY.md` records scope/hash/tolerances. Synthetic numerical parity does not imply physiological or full metric acceptance.
- Fresh combined app-host run (`root-bundled-context-integrated.log/.xcresult`) executes **280 tests with three failed assertions in two BackfillIdleWatchdog tests**. The new lossless quarantine path retains frames that old filter fixtures excluded; Maxwell owns contract/expectation repair. All consent, input, Swift context parity, repository ownership, account isolation, AI, readback and IMU/upload groups in that run pass. Do not describe the combined invocation as green. The original missing-fixture generation failure and external-volume fixture-read timeout remain recorded; the bundled resource fixes that test harness dependency.
- Both writer bindings now await the same captured `prepareImuPushSource()` before initial registration. The bootstrap opens the account membership index off-main; failure is not replaced with an empty/global/session-only source. Pauli's final exact-file archive/receipt/compaction helper reports 61 native tests and 54 NoopPush tests passed (`W5-EXACT-IMU-ARCHIVE-HANDOFF.md`); root's final 11 IMU/20 receipt app-host integration and independent receiver/archive review remain required.
- Shared schema fixtures explicitly record GRDB integer-rowid PRAGMA nullability versus Room's explicit NOT NULL declaration for syncJournalEntry.id. Root's seven actual GRDB schema-oracle tests pass (`root-rowid-schema-oracle.log`). This preserves deployed schemas; it does not weaken the schema comparison or rewrite migration history. Android rerun is outstanding.
- W4 server checkpoint reports 116 service passes, 963 kernel passes plus five existing skips, and successful installDist. It includes ordered historical generations and new metric/detail orchestration, but root's full migration chain and independent final review remain outstanding. Candidate auxiliary inputs, thermal calibration, some preference/consumer contracts and actual context producers are still local implementation work. Android's latest full checkpoint is 5,655 tests with four failures and six skips, not green; shared rounding and schema fixture follow-ups require rerun.

Each row remains open until implementation paths, migrations, behavior tests and evidence are attached. Native builds alone cannot satisfy runtime or device requirements.

| Requirement | Required outcome | Status | Files / migrations / tests / evidence / next action |
|---|---|---|---|
| W0.1 | Integrate live PR14/15/16 with migration history preserved | SOURCE VERIFIED | Nine cherry-picks above; Database.swift preserves both migration lines; SchemaOracleTests/IntegratedMigrationTests/PpgSchemaCompatibilityTests; protocol/store tests and phase-0 review. App/Android integration build still required. |
| W0.2 | Capture affected and unaffected deployment/install identity | EXTERNAL GATE | VALIDATION_RUNBOOK.md defines immutable build/account/endpoint/migration/image evidence. No affected or unaffected physical installation has been inspected in this implementation. Obtain authorized device/staging evidence. |
| W0.3 | Trace first stalled boundary on physical phone | EXTERNAL GATE | SyncPipelineTrace.swift and evidence validator distinguish commit/accept/archive/index/compute/display. Actual physical record trace remains unexecuted; procedure in VALIDATION_RUNBOOK.md. |
| W0.4 | Fail acceptance when deployment evidence is skipped | LOCAL REPAIR REVIEWED; EXTERNAL GATES OPEN | Commit2b017ec passes70 root and independent offline tests; supported exact migration-ledger normalization and reviewed full-container identity are closed. Real documented local native execution, immutable-image provenance and authorized deployment/device evidence remain separate; local success cannot report production readiness. |
| W1.1 | Session/ACK fences, schema compatibility and disable unsafe range skip | IMPLEMENTED; INTEGRATION ACTIVE | PR14/15 cherry-picks; BackfillActor, Backfiller, WhoopStore writer/fence, PPG compatibility migration; 735 protocol and 583 store passes, actor/watchdog app tests. Collector final drain and physical ACK trace remain. |
| W1.2 | Preserve record index through export/object/replay | IMPLEMENTED LANES; INTEGRATION ACTIVE | PPG1.3 and auxiliary1.4 binary/NDJSON identity, v51/Room42 keys, exact native scalar sender goldens and receiver/scorer admission. Protocol748, store607, sender69 and Edge72 tests above; Android actual PPG contributor provenance and full Section4 audit remain. |
| W1.3 | Gate offload on subscriptions/store and select restored source | SOURCE IMPLEMENTED; DEVICE GATE | BLEManager, Backfiller, SourceCoordinator, StorePaths; W1AccountIsolationTests and backfill suites. Physical restored-subscription/selected-device evidence unrun. |
| W1.4 | Lossless rejected-frame quarantine separate from diagnostics | IN IMPLEMENTATION | DurableIngest.swift, HistoricalStreams.swift, QuarantineArchive.swift and BLEManager. Bounded store admission exists; unconditional unknown/deep selection and receipt-backed archive are active, not accepted by optional raw capture alone. |
| W1.5 | Bound ingestion and progress; offload optional work | PARTIAL | BackfillActor/Collector bounded batching, FULL writer admission, retained-account capture and off-main checked IMU drain. Generic-source retirement remains P1; device low-disk/72-hour backlog tests outstanding. |
| W1.6 | Receipt-aware waveform/aux/raw/IMU retention | IMPLEMENTED LANES; IN REVIEW | Exact IMF1 session/continuous files, immutable membership/compaction, auxiliary sibling receipt binding and quarantine child archives. Independent34 native source/receipt tests plus root app cases; final clean integrated run, protocol-transition cleanup debt and physical storage behavior remain. No unverified source deletion permitted. |
| W2.1 | User/device upload credentials; quarantine legacy ownership | IMPLEMENTED; IN REVIEW | NoopPush AccountSession/AccountPushAdmission, CloudAuthClient/CloudAccountPersistence, account layouts and Android account package. Legacy populated unowned stores remain intact and inaccessible to a new owner; native identity/capture tests. |
| W2.2 | Validated identity and same-project readback/RLS | IMPLEMENTED; IN REVIEW | AccountScope, account HTTP transports, Edge ordinary-JWT intake, SQL ownership/RLS, ServerScoreClient; native Edge PG/PostgREST tests and typed receipt tests. Actual endpoint/user canary unrun. |
| W2.3 | Scope sessions, cursors, pending files and caches | IMPLEMENTED; REVIEW REPAIRS ACTIVE | AccountStorageLayout, CloudUploadJournal/ProgressStore, WhoopStore cache v48/owner v50 and Android Room/files. Fresh current-source129 targeted tests pass including Room43/restore/GPS, but independent review finds a further GPS captured-versus-raw HR mismatch and missing readout revision. Full suite and remaining lifecycle integrations are not yet accepted. |
| W2.4 | Fence logout/account/project switch and retain old debt | PARTIAL; REVIEW REPAIRS ACTIVE | Swift immutable runtime and retained generic/IMU drains have bounded native/app evidence. Android generic/IMU tails, GPS recovery, continuous upload binding, backup restore and profile final-commit guards require six assigned P1 repairs. Consent denial/full-queue controls are separately verified; they do not close these lifecycle defects. |
| W2.5 | Serialize token refresh and retry transient failures | IMPLEMENTED; IN REVIEW | NoopPush AccountSession and CloudAuthClient; generation-tagged refresh, receiver binding and native account/transport tests. Actual token expiration/background daemon tests unrun. |
| W2.6 | Document identity-preserving endpoint migration | DOCUMENTED; EXTERNAL GATE | VALIDATION_RUNBOOK.md requires project/auth user/device ID preservation or explicit migration mapping. No runtime endpoint migration or data copy was executed. |
| W3.1 | Generation-scoped failure budget/backoff/dead letter/repair | REVIEWED CHECKPOINT | ScoringWorkQueue.kt, migration 20260918010000, ScoringDurabilityIntegrationTest; >100 update native regression, a1f12dc and independent 12-test closure. W4 changes require final integrated rerun. |
| W3.2 | Atomic token-fenced claims and bounded concurrency | REVIEWED CHECKPOINT | ScoringWorkQueue, LeaseHeartbeat, ScoringPoller; native concurrent PG claim/expiry/publication tests; a1f12dc and independent closure. |
| W3.3 | Fence result publication and preserve arrivals | REVIEWED CHECKPOINT | EngineIngestWriter and SQL atomic publication; ScoringConcurrencyIntegrationTest; legacy overwrite repair closed independently. Historical checkpoint integration is new work. |
| W3.4 | Transactional invalidation and dependency-window reconciliation | PARTIAL | 010000 projection invalidation/repair and native tests. Ordered historical metric dependencies/profile/edits are W4 work in progress. |
| W3.5 | Verified object completion and resumable index repair | REVIEWED CHECKPOINT | e19be7b Edge durability/objects/workers plus 020000/040000; root and independent 59 tests/23 native steps pass. DURABILITY_RECEIPT.md defines hash/size/owner/resource contract; actual B2 permissions/schedule unverified. |
| W3.6 | Device result grain, deterministic source and sleep replacement | REVIEWED CORE; W4 ACTIVE | 010000 device-keyed snapshots/RPC, EngineIngestWriter, durability integration fixtures. 050000 stable edit identities and cross-day projections are native-tested; full restaging/history remains active. |
| W3.7 | Independent durable derived archive retries | REVIEWED CHECKPOINT | SnapshotArchiveWorker, DerivedArtifactWriter, 010000 archive jobs; ArchiveDurabilityIntegrationTest and W3 independent closure. External B2 deployment remains unverified. |
| W3.8 | Fair bounded backlog, metrics and query plans | PARTIAL | Queue claim bounds, HeartbeatReporter and native tests; representative fleet query plans/throughput remain unmeasured. |
| W4.1 | Inventory all derived metrics and consumers | INVENTORIED | METRIC_OWNERSHIP.md maps visible fields/producers/consumers and historical dependencies. Inventory is not migration or parity acceptance. |
| W4.2 | Server computation and ordered historical dependencies | IN IMPLEMENTATION | 050000 input/checkpoint SQL and232-service native checkpoint precede recent additions. Reset-gap repair independently closes warm/cold age mismatch; root100Swift tests pass. Fresh64-day raw populated context publication and seven Swift adapter tests pass. Complete raw-to-server-day Swift goldens and per-metric consumer acceptance remain local blockers. |
| W4.3 | Per-metric capability and activation gate | PARTIAL | ServerScoreSnapshot metric vocabulary, ServerScoringSettings/Repository and UI activation sets, account-scoped decisions. Missing values stay null; not all producers can be disabled until complete writer/parity validation. |
| W4.4 | Lifecycle/sign-in/day/upload-driven readback | IMPLEMENTED; IN REVIEW | ServerScoreRepository, AccountAppRuntime, AppModel foreground/day/upload hooks. Fresh-install fetch does not require local repo.today; lifecycle/cache tests and final device acceptance remain distinct. |
| W4.5 | Observable immutable snapshots and bounded async cache | IMPLEMENTED; IN REVIEW | ServerScoreViewState/Repository, WhoopStore ServerScoreCache v48, RepositoryServerScores; app-host authoritative-null/empty/stale and typed DTO tests. Main-thread/device cache latency unmeasured. |
| W4.6 | Complete sleep/stages/charts and server-visible edits | PARTIAL | ServerScoreSleepSession/Presentation, SleepView, ServerSleepInput/Repository, ScoringInputJournal/Coordinator, 050000. Five actual client edit tests pass; head/rebase and full server restaging/compact charts still active before UX activation. |
| W4.7 | Input/result revisions, provenance and freshness | IMPLEMENTED; INTEGRATION ACTIVE | Schema-2 DTO/cache/RPC, ServerScoreStatusNote, typed receipt/input revision and coverage/gaps; app DTO/history tests. Full-metric dependency provenance and end-to-end new-revision visibility still require acceptance. |
| W4.8 | Canonical RR selection and cross-platform fixtures | KERNEL V2 PASS; CURRENT V3 OPEN | Stored-code2 repair passes17 actualPG controls and independent source review. Corrected mode dispatch passes23 controls plus all13 immutable-v2 cases with all217 source hashes matching then. Five subsequent additive APIs require a new current-source corpus; full server-day/DST parity and actual paired input-selection sender qualification remain open. |
| W4.9 | Sleep fallback/grouping/timezone/DST parity | IN IMPLEMENTATION | DayScorer, SleepEditResolver, stable SleepIdentity and kernel local bounds; client DST earliest-day test passes. Server HR-only/split-night/DST output goldens and current extended native integration remain active. |
| W4.10 | Version negotiation and explicit reprocessing | REVIEWED CORE; INTEGRATION ACTIVE | 010000/030000 active algorithm registration/queue generations, schema-2 capability/unsupported state. W3 registration race closed independently; full next-version client/history fixture still required. |
| W5.1 | CoreBluetooth launch/restoration state machine | SOURCE IMPLEMENTED; DEVICE GATE | BLEManager launch restore identifier/delegates plus AccountAppRuntime, StrandApp/StrandiOSApp; source and account-fence app tests. OS restoration/reconnect proof unrun. |
| W5.2 | Durable file-backed background URLSession jobs and delegates | IMPLEMENTED; IN REVIEW | CloudUploadJournal/Queue/Session/BackgroundRuntime, captured account task maps and app delegate completion hub. Native real-source queue tests pass; actual process-death background daemon restoration remains unrun. |
| W5.3 | Receipt-safe task/file/cursor crash recovery | IMPLEMENTED; HOST REVIEW CLOSED; DEVICE GATE | Committed8457c72 core plus12647fc complete prepared selections retain original multipart payloads/progress, whole-group reservation, receipt association and exact cleanup. Independent live-error reproduction is repaired; root's fresh217-app run includes25 prepared plus2 original review cases, and78 package tests pass. Unknown/ambiguous debt stays fail-closed. OS daemon process-restoration remains a separate device gate. |
| W5.4 | Concurrent bounded catch-up upload preparation | PARTIAL | CloudPushWorker/Snapshot/Queue off-main bounded immutable jobs; actor commit notifications and coalesced reconciliation. Remaining lanes and real 72-hour throughput/backpressure evidence outstanding. |
| W5.5 | Explicit wait reasons and network policy | IMPLEMENTED; DEVICE GATE | CloudUploadSession/Queue state, CloudPushSettings Wi-Fi/cellular admission and account auth pauses. Controlled policy tests pass; actual radios/background opportunity tests unrun. |
| W5.6 | Opportunistic BGTask expiry and power/force-quit handling | SOURCE IMPLEMENTED; DEVICE GATE | CloudPushBackgroundScheduler/PeriodicScheduler, background runtime retirement/expiry, runbook force-quit and first-unlock procedures. Real locked/low-power/process-termination artifacts unrun. |
| W6.1 | Remove large reads/analytics from rendering | PARTIAL | Repository server overlays and conditional compact series, snapshot actor decoding. Today/Liquid residual raw scans and global local-rescore suppression remain blocked on complete per-metric replacements, not removed by a badge. |
| W6.2 | Bound live publication and diagnostic writers | PARTIAL | LiveState account log storage, bounded upload/progress state and raw capture writers. Broad view fan-out and all high-frequency instrumentation still need integrated review/device workload. |
| W6.3 | Correlated privacy-safe stage signposts | PARTIAL | SyncPipelineTrace opaque UUID intervals, BLE/store/upload/cache/publication stages and ServerScoreContentReadyTrace tests. Cached state readiness is NOT a rendered frame; hardware first-frame and complete record/job correlation unverified. |
| W6.4 | Instruments/MetricKit gates and approximate counter labeling | SOURCE IMPLEMENTED; DEVICE GATE | SyncMetricKitCollector.swift retains bounded local raw payloads; DisplayPerformanceMonitor labels 33 ms counter approximate; VALIDATION_RUNBOOK and evidence validator require actual aggregate Hitches/60/120 Hz artifacts. Final build and physical traces still required. |
| W6.5 | Thermal/power adaptations and physical device comparison | PARTIAL; DEVICE GATE | Upload scheduler/capture power controls and explicit optional-work policy require final source audit; essential pending data retained. Matched Release energy/thermal and actual refresh-rate comparisons unrun. |
| W7.1 | Additive staged rollout/canary/rollback runbooks | DOCUMENTED; REVIEW PENDING | VALIDATION_RUNBOOK.md plus hardened infra acceptance scripts. No deployment authorized or performed. |
| W7.2 | Cohort, overnight and multi-day gates with separate readiness claims | EXTERNAL GATE | Runbook specifies fresh/research/affected cohort, locked reconnect, overnight wake, multi-day transitions and separate readiness verdicts. Real artifacts required; unexecuted. |

## Cross-cutting acceptance

- Section 4 stream matrix has been inspected at `/Volumes/Untitled/whoop-w1-evidence-20260918-42qzUs/W1-SECTION4-STREAM-MATRIX.md`; copy its reviewed, final form into these task docs at integration. Open gaps include auxiliary same-second keying, scalar steps/sleep-state/PPG-HR lanes, unconditional quarantine archive, IMU resource membership and device/firmware/provenance completeness. Existing schema-registry classification alone is not proof of an actual sender lane.
- Every W0-W6 acceptance paragraph requires adversarial evidence, including >100 generations, stale workers, account changes, duplicate/out-of-order chunks, partial object completion and stale readback.
- Section 6 performance targets remain UNMEASURED: 60/120 Hz deadlines, aggregate Hitches <=10 ms/s, no reproducible main-thread stalls >=250 ms, warm cache <=100 ms p95, cold cache <=1 s p95, commit-to-display <=120 s p95, ready-result readback <=5 s or <=60 s fallback.
- The 72-hour backlog capacity, two-hour locked reconnect, overnight wake and matched energy/thermal comparisons require physical-device artifacts.
- No actual endpoint, device/account mapping, installed build/schema, server image, migration ledger, advancing heartbeat or canary has been verified. Device/staging/production gates remain explicit and cannot be inferred from source tests.
- Acceptance requiring production writes is outside current authorization. Prepare procedures locally without executing them.
